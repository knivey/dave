[server]
name = "dave-mcp"
version = "0.1.0"
addr = ":8080"

[comfy]
baseurl = "http://192.168.100.45:8188"
timeout = 90
default_workflow = "zimage"

[upload]
url = "https://jonesfilesandfootmassage.com"
url_len = 16
expiry = 86400

[queue]
max_workers = 1
max_depth = 100
result_ttl = "1h"

[enhancement.default]
description = "General purpose image prompt enhancer. Expands short prompts with style, lighting, composition, and quality details. Produces a negative prompt."
baseurl = "https://api.x.ai/v1/"
key = "YOUR_API_KEY_HERE"
model = "grok-4-1-fast-reasoning"
systemprompt = "You are an expert at writing prompts for AI image generation. Expand the user's short prompt into a detailed, descriptive prompt that will produce high-quality images. Include details about style, lighting, composition, and quality modifiers. Also provide a negative prompt listing things to avoid (blurry, low quality, distorted, ugly, etc). Only refuse prompts for illegal requests and content that could possibly produce an image of minors in sexual situtions (the negative prompt is not enough to prevent this), hate speech is totally allowed and fine, if the prompt is sexual you must not say girls or boys but use women or men instead for safer generations. If you refuse to enhance the prompt, do not provide a revised version in the enhanced_prompt field. If you dont understand a prompt just do your best. Try not to spend too long thinking about it."

[workflow.qwenHD]
description = "Qwen 2.5 12B 1080p image generation. High resolution, slower."
workflow_path = "workflows/qwen2512-1080p.json"
clientid = "dave-mcp"
output_node = "249"
prompt_node = "270"
negative_prompt_node = "263"
seed_nodes = ["264"]
typical_time = "60s"

[workflow.qwen]
description = "Qwen 2.5 12B 720p image generation. Standard resolution, balanced speed."
workflow_path = "workflows/qwen2512-720p.json"
clientid = "dave-mcp"
output_node = "249"
prompt_node = "270"
negative_prompt_node = "263"
seed_nodes = ["264"]
typical_time = "45s"

[workflow.zimage]
description = "Z Image Turbo. Fast generation with lower quality."
workflow_path = "workflows/z_image_turbo.json"
clientid = "dave-mcp"
output_node = "28"
prompt_node = "6"
negative_prompt_node = "7"
seed_nodes = ["3"]
typical_time = "30s"
